\begin{notation}[{\cite[Section 1]{ekedahl_af}}] \label{notation:ekedahl_af_topos_of_inverse_systems}
%     Let $(S,R)$ be a \CrefAndHyperrefIfExist{definition:ringed_topos}{ringed topos} and let $\mathfrak{m} \subseteq R$ be a \CrefAndHyperrefIfExist{definition:sheaf_of_ideals_for_a_sheaf_of_rings_on_a_site}{two-sided ideal of $R$}. 
%     \begin{enumerate}
%         \item Let \hl{$S^\bbN$} denote the topos of inverse systems $\{M_n, n > 0, \pi_n: M_{n+1} \to M_n\}$ of objects of $S$. We equip it with the structure of a ringed topos with ring 
%         $$R_\bullet: \cdots \to R_n \xrightarrow{\text{proj}} R_{n-1} \to \cdots \to R_1 \to 0 $$
%         where $R_n = R/\mathfrak{m}^n$. 
%         \item Let \hl{$\pi: S^\bbN \to S$} denote the topos morphism such that $\pi_* M_\bullet = \varprojlim M_\bullet$ and $\pi^* M$ is the constant inverse system
%         $\cdots \xrightarrow{\id} M \xrightarrow{\id} M \xrightarrow{\id} \to M \to 0$.
%         \item For $n > 0$, let \hl{$i_n: S \to S^\bbN$} denote the topos moprhism such that $i_n^* M_\bullet = M_n$.
%     \end{enumerate}
% \end{notation}