\begin{lemma}[Tail of convergent series tends to zero] \label{lemma:tail_of_convergent_series_tends_to_zero}
    \TODO{AI generated, verify}
    Let $V$ be a \CrefAndHyperrefIfExist{definition:extended_norm_on_a_vector_space_over_a_field_with_absolute_value}{normed vector space} over $\mathbb{R}$\CrefIfExists{definition:field_of_real_numbers} or $\mathbb{C}$\CrefIfExists{definition:field_of_complex_numbers}, with norm $\|\cdot\|$. Let $(a_n)_{n=0}^\infty$ be a sequence in $V$, and suppose that the series $\sum_{n=0}^\infty a_n$ \CrefAndHyperrefIfExist{definition:infinite_series_and_convergence_in_a_topological_abelian_group}{converges} to $S$.

    Let $T_N = S - \sum_{n=0}^N a_n$ denote the tail after $N$ terms. Then $\lim_{N \to \infty} T_N = 0$, i.e., for every $\varepsilon > 0$ there exists $N_0 \geq 0$ such that $\|T_N\| < \varepsilon$ for all $N \geq N_0$.
\end{lemma}

\begin{proof}
    \TODO{AI generated, verify}
    Let $S_N = \sum_{n=0}^N a_n$ denote the partial sums. By hypothesis, $\sum_{n=0}^\infty a_n$ converges to $S$, which means that the sequence of partial sums $(S_N)$ converges to $S$ in $V$\CrefIfExists{definition:convergence_and_limit_of_sequence_in_extended_metric_space_valued_in_ordered_commutative_monoid_with_adjoined_infinity}.

    Convergence of $(S_N)$ to $S$ means that for every $\varepsilon > 0$ there exists $N_0 \geq 0$ such that $\|S_N - S\| < \varepsilon$ for all $N \geq N_0$.

    Observe that $T_N = S - S_N$, so $\|T_N\| = \|S - S_N\| = \|S_N - S\|$, where the last equality follows from absolute homogeneity of the norm (i.e., $\|-v\| = \|v\|$ for all $v \in V$)\CrefIfExists{definition:extended_norm_on_a_vector_space_over_a_field_with_absolute_value}. Therefore for every $\varepsilon > 0$ there exists $N_0 \geq 0$ such that $\|T_N\| < \varepsilon$ for all $N \geq N_0$, which is exactly $\lim_{N \to \infty} T_N = 0$.
\end{proof}
