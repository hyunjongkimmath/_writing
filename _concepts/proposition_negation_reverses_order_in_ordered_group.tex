\begin{proposition} \label{proposition:negation_reverses_order_in_ordered_group}
    \TODO{AI generated, verify}
    Let $G$ be an \CrefAndHyperrefIfExist{definition:ordered_abelian_group}{ordered abelian group}. For any $a,b \in G$,
    $$a \le b \iff -b \le -a.$$
    In particular, negation is an order-reversing involution.
\end{proposition}
\begin{proof}
    \TODO{AI generated, verify}
    If $a \le b$, then adding $-b$ to both sides gives $a - b \le 0$. Adding $-a$ gives $-b \le -a$. The converse follows by applying the same argument to $-b \le -a$ and using \Cref{corollary:inverse_of_inverse_in_group_is_element} (for the additive group, $-(-x) = x$).
\end{proof}