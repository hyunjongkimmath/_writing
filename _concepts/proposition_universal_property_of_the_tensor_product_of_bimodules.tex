\begin{proposition}[Universal Property of the Tensor Product of Bimodules] \label{proposition:universal_property_of_the_tensor_product_of_bimodules}
Let $R,S,T$ be \CrefAndHyperrefIfExist{definition:ring}{(not necessarily commutative) rings}. Let $M$ be an \CrefAndHyperrefIfExist{definition:bimodule_over_rings_module_over_a_ring}{$R$-$S$ bimodule} and let $N$ be an $S$-$T$ bimodule. Let $P$ be an $R$-$T$ bimodule. Then for every \CrefAndHyperrefIfExist{definition:multilinear_map_of_modules_over_rings}{$R$-$T$ bilinear map}
$$ \beta : M \times N \to P, $$
that is, a map satisfying
\begin{align*}
\beta(m+m',n) &= \beta(m,n) + \beta(m',n), \\
\beta(m,n+n') &= \beta(m,n) + \beta(m,n'), \\
\beta(r \cdot m, n) &= r \cdot \beta(m,n), \\
\beta(m,n\cdot t) &= \beta(m,n)\cdot t, \\
\beta(m \cdot s, n) &= \beta(m,s \cdot n),
\end{align*}
for all $m,m' \in M$, $n,n' \in N$, $r \in R$, $s \in S$, $t \in T$, there exists a unique $R$-$T$ bimodule homomorphism
$$ \widetilde{\beta} : M \otimes_S N \to P $$
such that $\widetilde{\beta}(m \otimes n) = \beta(m,n)$ for all $m \in M$, $n \in N$.
\end{proposition}

\begin{proof}
    \TODO{AI generated, verify}
    Let $\pi : \mathbb{Z}[M \times N] \to M \otimes_S N = \mathbb{Z}[M \times N]/U$ be the \CrefAndHyperrefIfExist{theorem:canonical_projection_from_group_to_quotient_by_normal_subgroup}{canonical projection} onto the quotient by the subgroup $U$ generated by the bilinearity relations\CrefIfExists{definition:tensor_product_of_bimodules_of_rings}.

    We first construct the homomorphism $\widetilde{\beta}$. Since $P$ is an abelian group under addition, we view $\beta$ as a map of sets $\beta : M \times N \to P$. By the universal property of the \CrefAndHyperrefIfExist{definition:free_abelian_group_generated_by_a_set}{free abelian group} $\mathbb{Z}[M \times N]$, there exists a unique group homomorphism
    $$\hat{\beta} : \mathbb{Z}[M \times N] \to P$$
    extending $\beta$, i.e., $\hat{\beta}((m,n)) = \beta(m,n)$ for all $(m,n) \in M \times N$.

    We show that $U \subseteq \operatorname{ker}(\hat{\beta})$. The subgroup $U$ is generated by elements of the form
    \begin{align*}
    &(m+m',n) - (m,n) - (m',n), \\
    &(m,n+n') - (m,n) - (m,n'), \\
    &(m \cdot s, n) - (m, s \cdot n),
    \end{align*}
    for all $m,m' \in M$, $n,n' \in N$, and $s \in S$. Since $\hat{\beta}$ is a group homomorphism, it suffices to check that each generator maps to zero in $P$. We have
    \begin{align*}
    \hat{\beta}\bigl((m+m',n) - (m,n) - (m',n)\bigr) &= \beta(m+m',n) - \beta(m,n) - \beta(m',n) = 0_P, \\
    \hat{\beta}\bigl((m,n+n') - (m,n) - (m,n')\bigr) &= \beta(m,n+n') - \beta(m,n) - \beta(m,n') = 0_P, \\
    \hat{\beta}\bigl((m \cdot s, n) - (m, s \cdot n)\bigr) &= \beta(m \cdot s, n) - \beta(m, s \cdot n) = 0_P,
    \end{align*}
    where the vanishing follows from the additivity of $\beta$ in each argument and the balancing condition $\beta(m \cdot s, n) = \beta(m, s \cdot n)$, respectively.

    Since $U \subseteq \operatorname{ker}(\hat{\beta})$, we may apply the \CrefAndHyperrefIfExist{lemma:unique_factorization_of_group_homomorphism_through_quotient_by_normal_subgroup_contained_in_kernel}{factorization lemma for group homomorphisms through quotients}. This yields a unique group homomorphism
    $$\widetilde{\beta} : M \otimes_S N \to P$$
    such that $\widetilde{\beta} \circ \pi = \hat{\beta}$, i.e., $\widetilde{\beta}(m \otimes n) = \beta(m,n)$ for all $m \in M$, $n \in N$.

    Next we verify that $\widetilde{\beta}$ is an $R$-$T$ bimodule homomorphism. For any $r \in R$, $t \in T$, and pure tensor $m \otimes n \in M \otimes_S N$, we have
    \begin{align*}
    \widetilde{\beta}(r \cdot (m \otimes n)) &= \widetilde{\beta}\bigl((r \cdot m) \otimes n\bigr) = \beta(r \cdot m, n) = r \cdot \beta(m,n) \\
    &= r \cdot \widetilde{\beta}(m \otimes n), \\
    \widetilde{\beta}((m \otimes n) \cdot t) &= \widetilde{\beta}\bigl(m \otimes (n \cdot t)\bigr) = \beta(m, n \cdot t) = \beta(m,n) \cdot t \\
    &= \widetilde{\beta}(m \otimes n) \cdot t,
    \end{align*}
    where the first equality in each line uses the $R$-$T$ bimodule structure on $M \otimes_S N$\CrefIfExists{definition:tensor_product_of_bimodules_of_rings}, and the second equality uses the $R$-linearity and $T$-linearity of $\beta$. Since every element of $M \otimes_S N$ is a finite sum of pure tensors and both $\widetilde{\beta}$ and the module actions are additive, these equalities extend to all of $M \otimes_S N$. Therefore, $\widetilde{\beta}$ is an $R$-$T$ bimodule homomorphism.

    Finally we establish uniqueness. Suppose that $\gamma : M \otimes_S N \to P$ is another $R$-$T$ bimodule homomorphism satisfying $\gamma(m \otimes n) = \beta(m,n)$ for all $m \in M$, $n \in N$. Since every element of $M \otimes_S N$ is a finite sum of pure tensors and both $\widetilde{\beta}$ and $\gamma$ are additive, we have $\gamma(x) = \widetilde{\beta}(x)$ for all $x \in M \otimes_S N$. Hence, $\gamma = \widetilde{\beta}$.
\end{proof}
