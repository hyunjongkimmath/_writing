\begin{proposition} \label{proposition:subtraction_preserves_order_in_ordered_group}
    \TODO{AI generated, verify}
    Let $G$ be an \CrefAndHyperrefIfExist{definition:ordered_abelian_group}{ordered abelian group}. For any $a,b,c \in G$,
    $$a \le b \iff a - c \le b - c \iff a + c \le b + c.$$
    In particular, subtraction and addition are order-preserving.
\end{proposition}
\begin{proof}
    \TODO{AI generated, verify}
    By definition of an ordered abelian group, addition preserves order: $a \le b \implies a + c \le b + c$. For subtraction, write $a - c = a + (-c)$ and apply addition preservation twice.
\end{proof}