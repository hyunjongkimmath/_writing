\begin{proposition}[Absolute convergence implies convergence in a Banach space over $\mathbb{R}$ or $\mathbb{C}$] \label{proposition:absolute_convergence_implies_convergence_of_series_in_a_normed_vector_space_over_R_or_C}
    \TODO{AI generated, verify}
    Let $V$ be a \CrefAndHyperrefIfExist{definition:banach_space_over_a_commutative_ring_with_an_absolute_value_valued_in_an_ordered_semiring}{Banach space} over $\mathbb{R}$\CrefIfExists{definition:field_of_real_numbers} or $\mathbb{C}$\CrefIfExists{definition:field_of_complex_numbers}, with norm $\|\cdot\|$. Let $(v_n)_{n=1}^\infty$ be a sequence in $V$.

    If the series of real numbers $\sum_{n=1}^\infty \|v_n\|$ converges in $\mathbb{R}$, i.e., if $\sum_{n=1}^\infty v_n$ \CrefAndHyperrefIfExist{definition:absolute_convergence_of_a_series_in_module_over_a_commutative_ring_with_an_absolute_value_valued_in_an_ordered_semiring}{converges absolutely}, then the series $\sum_{n=1}^\infty v_n$ \CrefAndHyperrefIfExist{definition:infinite_series_and_convergence_of_infinite_series_in_a_topological_abelian_semigroup}{converges} in $V$.
\end{proposition}

\begin{proof}
    \TODO{AI generated, verify}
    Let $S_N = \sum_{n=1}^N v_n$ denote the partial sums. We show that $(S_N)$ is a Cauchy sequence in $V$\CrefIfExists{definition:cauchy_sequence_in_an_extended_metric_space_valued_in_ordered_commutative_monoid_with_adjoined_infinity}, and then invoke completeness of $V$.

    The function $d(x,y) = \|x - y\|$ defines an \CrefAndHyperrefIfExist{definition:extended_metric_induced_by_an_extended_norm_on_a_module_over_a_commutative_ring_with_absolute_value}{extended metric} on $V$\CrefIfExists{definition:extended_metric_on_a_set_valued_in_an_ordered_commutative_monoid_with_adjoined_infinity}. A sequence in $V$ is Cauchy with respect to this metric if and only if for every $\varepsilon > 0$ there exists $N_0 \geq 1$ such that $\|S_M - S_K\| < \varepsilon$ for all $M, K \geq N_0$.

    Let $\varepsilon > 0$ be given. Since $\sum_{n=1}^\infty \|v_n\|$ converges in $\mathbb{R}$, the sequence of partial sums $T_M = \sum_{n=1}^M \|v_n\|$ is a Cauchy sequence in $\mathbb{R}$. Equivalently, by the Cauchy criterion for series of real numbers$\CrefIfExists{proposition:cauchy_criterion_for_convergence_of_series_of_real_numbers}$, there exists $N_0 \geq 1$ such that for all $M \geq N_0$:
    $$\sum_{n=N_0+1}^M \|v_n\| < \varepsilon.$$

    For any $M \geq K \geq N_0$, we have:
    $$S_M - S_K = \sum_{n=K+1}^M v_n.$$

    By the triangle inequality for the norm\CrefIfExists{definition:extended_norm_on_a_vector_space_over_a_field_with_absolute_value}:
    $$\|S_M - S_K\| = \left\|\sum_{n=K+1}^M v_n\right\| \leq \sum_{n=K+1}^M \|v_n\|.$$

    Since $K + 1 > N_0$, the tail sum $\sum_{n=K+1}^M \|v_n\|$ is bounded by the Cauchy criterion:
    $$\sum_{n=K+1}^M \|v_n\| \leq \sum_{n=N_0+1}^M \|v_n\| < \varepsilon.$$

    Therefore $\|S_M - S_K\| < \varepsilon$ for all $M, K \geq N_0$, showing that $(S_N)$ is a Cauchy sequence in $V$. Since $V$ is a Banach space, it is complete\CrefIfExists{definition:banach_space_over_a_commutative_ring_with_an_absolute_value_valued_in_an_ordered_semiring}, so every Cauchy sequence converges. Hence $S_N \to v$ for some $v \in V$, which means $\sum_{n=1}^\infty v_n$ converges in $V$.
\end{proof}
