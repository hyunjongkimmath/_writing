\begin{lemma}[Nakayama's lemma with nilpotent ideal]  \label{lemma:nakayamas_lemma_for_finitely_generated_modules_over_a_ring_with_nilpotent_ideal}
	\TODO{AI generated, verify}

	Let $R$ be a \CrefAndHyperrefIfExist{definition:ring}{ring}, $M$ a right $R$-module (not assumed finitely generated), and $I$ a left ideal of $R$ that is \CrefAndHyperrefIfExist{definition:nilpotent_ideal_of_a_ring}{nilpotent}.

	If $IM = M$, then $M = 0$.
\end{lemma}
\begin{proof}
	\TODO{AI generated, verify}
	Suppose that $IM = M$. Since $I$ is nilpotent, there exists some integer $k \geq 1$ such that $I^k = 0$.

	We claim that $I^j M = M$ for all $j \geq 1$. For $j = 1$, this is the hypothesis. If $I^j M = M$, then
	$$I^{j+1} M = I(I^j M) = IM = M.$$

	In particular, $I^k M = M$. Since $I^k = 0$, we have $M = 0 \cdot M = 0$. Therefore, $M = 0$.
\end{proof}
