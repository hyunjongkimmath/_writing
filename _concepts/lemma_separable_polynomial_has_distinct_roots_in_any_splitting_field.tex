\begin{lemma} \label{lemma:separable_polynomial_has_distinct_roots_in_any_splitting_field}
    \TODO{AI generated, verify}
    Let $k$ be a \CrefAndHyperrefIfExist{definition:field}{field} and let $f \in k[x]$ be a non-constant \CrefAndHyperrefIfExist{definition:separable_polynomial_over_a_field}{separable polynomial over $k$}. If $L$ is a \CrefAndHyperrefIfExist{definition:splitting_field_of_polynomials_over_a_field_normal_field_extension}{splitting field} of $f$ over $k$, then $f$ has exactly $\deg(f)$ distinct roots in $L$.
\end{lemma}
\begin{proof}
    \TODO{AI generated, verify}
    Let $d = \deg(f)$. Over any algebraic closure $\overline{k}$ of $k$, the polynomial $f$ factors as
    $$f(x) = c \prod_{i=1}^d (x - \alpha_i)$$
    with $c \in k^\times$ and $\alpha_i \in \overline{k}$. Since $f$ is separable by \Cref{definition:separable_polynomial_over_a_field}, all its roots in $\overline{k}$ are simple, so the $\alpha_i$ are pairwise distinct.

    By definition of a splitting field, $L$ contains all roots $\alpha_i$ of $f$ and is generated by them over $k$. Hence the $\alpha_i$ lie in $L$, they are pairwise distinct, and they constitute all roots of $f$ (up to multiplicity, each being simple). Therefore $f$ has exactly $d$ distinct roots in $L$.
\end{proof}