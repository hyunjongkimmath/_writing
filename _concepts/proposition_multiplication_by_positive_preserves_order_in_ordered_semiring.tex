\begin{proposition} \label{proposition:multiplication_by_positive_preserves_order_in_ordered_semiring}
    \TODO{AI generated, verify}
    Let $R$ be an \CrefAndHyperrefIfExist{definition:ordered_semiring}{ordered semiring}. For any $a,b \in R$ and $c \in R_{\ge 0}$ (the positive cone of $R$),
    $$a \le b \implies a \cdot c \le b \cdot c \quad\text{and}\quad c \cdot a \le c \cdot b.$$
\end{proposition}
\begin{proof}
    \TODO{AI generated, verify}
    By definition of an ordered semiring (see \Cref{definition:ordered_semiring}), the order is compatible with multiplication by non-negative elements: if $a \le b$, then $a \cdot c \le b \cdot c$ and $c \cdot a \le c \cdot b$ for all $c \ge 0$.
\end{proof}