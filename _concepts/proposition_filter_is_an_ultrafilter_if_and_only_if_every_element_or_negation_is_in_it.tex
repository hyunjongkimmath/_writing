\begin{proposition} \label{proposition:filter_is_an_ultrafilter_if_and_only_if_every_element_or_negation_is_in_it}
Let $B$ be a Boolean algebra and $U \subseteq B$ a filter. $U$ is an ultrafilter if and only if for every $x \in B$, either $x \in U$ or $\neg x \in U$.
\end{proposition}