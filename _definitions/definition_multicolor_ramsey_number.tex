\begin{definition} \label{definition:multicolor_ramsey_number}
\TODO{AI generated, verify}
Let $m$ be a positive integer, and let $k_1, k_2, \dots, k_m$ be positive integers. The \hldef{multicolor Ramsey number $R(k_1, k_2, \dots, k_m)$}, denoted by \hl{$R(k_1, k_2, \dots, k_m)$}, is the smallest integer $N$ such that every coloring of the edges of the complete graph $K_N$ with $m$ colors contains a monochromatic complete subgraph on $k_i$ vertices in color $i$, for some $i \in \{1, 2, \dots, m\}$.

When $m = 2$, this reduces to the two-color Ramsey number $R(k_1, k_2)$.
\end{definition}
