\begin{definition} \label{definition:idempotent_linear_map_and_projection_operator}
    Let $F$ be a \CrefAndHyperrefIfExist{definition:field}{field}, and let $V$ be an \CrefAndHyperrefIfExist{definition:vector_space_over_a_field}{$F$-vector space}.

    A linear endomorphism $P: V \to V$ is called \hldef{idempotent} (or a \hldef{projection operator}) if $P^2 = P$.

\TextIfExists{definition:idempotent_element_of_a_ring}{An idempotent linear map is precisely an \Cref{definition:idempotent_element_of_a_ring} in the ring $\operatorname{End}_F(V)$.}
\TODO{AI generated, verify}
\end{definition}
