
\begin{definition}\label{definition:commutative_binary_operation}
    Let $G$ be a set and let $\cdot: G \times G \to G$ be a binary operation. The operation $\cdot$ is called \hldef{abelian} or \hldef{commutative} if for all $g,h \in G$, we have 
    $$g \cdot h = h \cdot g.$$
\end{definition}
